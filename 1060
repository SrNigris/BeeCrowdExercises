#include <stdio.h>

int main()
{
    float n;
    int i, nPositivo;
    i = nPositivo = 0;
    
    while(i < 6){
        scanf("%f", &n);
        if(n > 0)
            nPositivo = nPositivo + 1;
        i++;
    }
    
    printf("%i valores positivos\n", nPositivo);
    
    return 0;
}
